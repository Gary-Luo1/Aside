# 更新日志

本项目遵循[语义化版本](https://semver.org/lang/zh-CN/)。

## [0.3.0] - 2026-08-29

### 新增

- **卡片内就地配置**：未配置接口或缺少站点权限时，解释卡片里直接填写接口地址、密钥和模型名称，点「保存并解释」后立即生效并重发解释，全程不离开当前页。
- 单元测试：76 例，覆盖选词状态机、选词清洗、解释解析、卡片定位、主机权限、提示词隔离、请求协调。
- `npm run lint`（oxlint）与 `npm run format:check`（prettier）接入 CI。
- `npm run extension:pack` 一键产出可分发 zip。

### 修复

- **「打开设置」按钮无效**：content script 的 `chrome.runtime` 不提供 `openOptionsPage`，点击后毫无反应。改为就地配置后不再需要跳转。
- 密钥前后带空格时，连接测试成功但保存按钮仍不可用。
- 设置页的加载、保存、删除三处缺少异常处理，失败后界面没有任何反馈。
- 连接测试缺少超时兜底，后台无响应时按钮会一直保持禁用。
- 模型返回超长解释时整条作废，现在改为截断保留。
- 选词清洗未剥离英文引号，选中 `"API"` 时引号会一起发出。
- 遗留设置清理逻辑在每次 service worker 唤醒时重复执行，改为一次性迁移标记。

### 性能

- 手绘字体子集化到实际用到的 5 个字形：19.9 KB → 1.3 KB，content.js 从 72 KB 降到 53 KB。
- 拖选与滚动不再每次事件都强制读取布局（此前高频触发强制重排）。
- 零尺寸的隐藏 iframe 不再注册任何监听器。
- 卡片重挂载改为惰性检查，移除常驻的 MutationObserver。
- 继续解释入口的位置推算改为一次性取矩形，不再逐个候选触发重排。
- 同一词重复解释命中缓存，避免重复计费。

### 安全与合规

- 提示词隔离移除选中文本中的所有尖括号，缩小提示词注入面。
- 清理 `optional_host_permissions` 中被 `:*` 覆盖的冗余项。
- 补全 Gochi Hand 的 SIL OFL 许可声明与版权信息。

### 变更

- 后台解释请求增加全局并发上限（4）。
- TypeScript 编译选项加严：未使用变量与参数、switch 贯穿、隐式覆写、可选属性精确类型等。
- 设置页元素查询改用 `mustGet`，缺失时给出明确报错。
- 最低 Node 版本要求 22.6（内置测试运行器需要类型剥离）。
- `AbortSignal.any` 增加降级实现（Chrome 116 才支持，恰为本扩展的最低版本）。
- 关闭解释卡片后把焦点归还给原来的元素。

## [0.2.0] - 2026-08-23

首个公开版本：划词解释、专业与通俗两层结果、卡片内继续划词、禁止选择的网页上恢复选区。
